%h2= t ".heading"

= semantic_form_for [:admin, resource], html: { class: "form-horizontal" } do |form|
  = render "form", form: form

  = form.actions do
    = form.action :submit, button_html: { class: "btn btn-primary" }
