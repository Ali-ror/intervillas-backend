.row
  .col-md-2{ style: "hyphens: auto" }
    %h1.page-header Tags
    = render "sidebar"

  .col-md-10
    .not-a-table.js-sticky-header.mt-4.mb-5
      .header
        .cell Kategorie
        .cell Amenity-ID
        .cell
          %span.fi.fi-de
          Beschreibung
        .cell
          %span.fi.fi-us
          Beschreibung
        .cell.text-center Kardi-<br>nalität
        .cell
          %span.fi.fi-de
          Lokalisierung
        .cell
          %span.fi.fi-us
          Lokalisierung
        .cell.text-center Filter?
        .cell

      - cid = tags_form[0].category_id
      - tags_form.each do |form|
        - if cid != form.category_id
          -# add a spacer between categories
          - cid = form.category_id
          .not-a-row
            .cell.small &nbsp;
        = render "tag_form", form: form
