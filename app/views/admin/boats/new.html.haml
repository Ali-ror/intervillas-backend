= render layout: "layout" do
  %h1.page-header Boot anlegen

  = semantic_form_for [:admin, resource] do |form|
    = render "form", form: form
    = form.actions do
      = form.action :submit, button_html: { class: "btn btn-primary" }

