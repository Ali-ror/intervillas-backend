.nested-fields.well
  = form.hidden_field :_destroy, value: 0

  = form.input :amount
  = form.input :value
  = form.input :text, hint: t(".text_hint", lang: customer_language)

  .form-group
    .col-sm-8.col-sm-offset-4
      = link_to_remove_association t(".remove_charge")
