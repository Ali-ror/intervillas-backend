%h2= t ".heading"

= semantic_form_for resource, url: [:admin, :specials], html: { class: "form-horizontal" } do |form|
  = render "form", form: form
  = form.actions do
    = form.action :submit, button_html: { class: "btn btn-primary" }

.mt-5
